# irc_sv_to_en
IRC translation bot with Azure Translate API

Translates swedish to english from a channel to another.
May be used for other languages.

ENV file should be .env in the bots directory and "chmod 600 .env" by root.

Use a systemctl service file to proper run the bot.

/ramses
